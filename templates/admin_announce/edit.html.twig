{% extends 'base.html.twig' %}

{% block title 'Gérer les biens' %}

{% block body %}
    <div class="container mt-4">
    <h2 style="text-align: center">Editer une Annonce</h2>
    {{ form_start(form) }}
    <div class="row">
        <div class="col-md-4">{{ form_row(form.title) }}</div>
        <div class="col-md-4">{{ form_row(form.adresse) }}</div>
        <div class="col-md-4">{{ form_row(form.price) }}</div>
    </div>
    <div class="row">
        <div class="col-md-4">{{ form_row(form.ImageCover) }}</div>
        <div class="col-md-4">{{ form_row(form.rooms) }}</div>
        <div class="col-md-4">{{ form_row(form.description) }}</div>
    </div>
    
    <div class="row-3">{{ form_row(form.isAvailable) }}</div>
    
    
    {# <div rows="3">{{ form_row(form.description) }} </div> #}
        <div class="btn btn-primary">Editer</div>
    {{form_end(form) }}
    </div>
    {% endblock %}